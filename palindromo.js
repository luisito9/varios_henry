
let random=0;
array = [];

function arreglo() {
    while (array.length < 10){
       num_random = Math.floor(Math.random() * 100)
       array.push(num_random);
    }
    return
}
arreglo();
console.log(array)

